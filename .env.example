# Session encryption (generate: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-long-random-string-at-least-32-chars

# Super admin credentials (only this user can manage admin accounts)
SUPER_ADMIN_USERNAME=Assad matoug
SUPER_ADMIN_PASSWORD=Assad5202320

# SQLite database path (relative to project root)
DATABASE_PATH=./data/clinic.db
